{
  "name": "stock-portfolio-api",
  "version": "1.0.0",
  "private": true,
  "description": "Vercel serverless functions for stock-portfolio Flutter app",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test 'api/*.test.mjs'"
  }
}
